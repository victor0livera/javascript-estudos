/*
Conhecemos de outras linguagens if, else, switch/case. A sintaxe no
JavaScript é idêntica à da linguagem C.


// bloco if/ else

let justDolt = 'try';

if (justDolt === 'try') {
    console.log('try nicee')
} else {
    console.log('hay')
}

// bloco switch

let justDolt = 'try'

*Um bloco switch/case basicamente serve como uma cadeia de if, elseif, else.*

switch (justDolt) {
    case 'try': console.log('eiei')
    break;
    
    default: console.log('erou')
    break;
}

*/
